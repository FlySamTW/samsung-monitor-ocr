# Samsung 通路陳列照片 OCR 專案指令

## 語言（最高優先）
- 所有輸出必須使用**繁體中文（台灣用語）**。
- 除非使用者明確要求，否則禁止出現簡體中文或英文。
- 技術名詞盡量使用台灣慣用詞，例如：
  - 螢幕（不用「屏幕／显示器」）
  - 資訊（不用「信息」）
  - 照片（不用「图片」）
  - 中央、左側、右側（不用「中间／左边／右边」）
  - 一台（不用「一臺」）

## 模型獨白（self-talk）規則
- 若需要產生模型獨白，必須以「我看到本輪結論：」開頭，先明確寫出
  遠景／單機、型號與價格，再簡述可見依據，最後以完整句號結束。
- 禁止使用「所以……」、連續省略號或把結論留到最後；主管可見介面不得
  出現賣關子式文字。
- 獨白中不得使用項目符號、星號或英文。
- 獨白中不得出現「用戶希望我分析」之類的後設評論。

## 輸出風格
- 簡潔、直接、可操作。
- 步驟與清單使用繁體中文編號。
- 除非必要，不要過度解釋已完成的動作。

## 專案背景
- 本專案為「三星電腦螢幕通路陳列照片自動 OCR + HITL 校正」。
- 處理流程：讀取門市照片 → 判斷遠景/單機 → 提取型號與價格 → 與 Samsung 官網價格比對 → 人工檢視與校正。
- 主要程式碼：`samsung_ocr_batch_processor.py`、`dashboard/src/App.jsx`、以及 `skills/` 目錄下的模組。
- 批次啟動指令為 `run_ocr.bat`；修改後需重新 build `dashboard/dist/` 才能反映前端變更。

## 持續運轉鐵律
- Dashboard、正式 OCR 與逐張上傳必須持續正常運轉；修正與稽核應在照片邊界、背景無視窗執行，不得反覆開終端機、瀏覽器視窗或另一套正式程序。
- 單張照片的視角、型號、價格或 FollowMe 內容衝突，只能限制在該張照片內，最多完成三次獨立且無記憶的模型呼叫後如實定案並排入上傳；不得因單張內容疑義停止整個資料夾，也不得出現第 4 輪以上的內容重試。
- 只有可證明會污染後續多張照片的系統性技術問題（例如請求與照片綁定失效、跨照片記憶污染、提示詞或證據版本整體失配）才可全域停止。恢復前必須保留證據、修正根因並通過回歸測試，不能只清除警示。
- 每張定案後立即進入可續傳佇列並上傳至年份資料夾；網路慢或暫時中斷只能形成持久化待上傳佇列，不得拖停 OCR，也不得等整年完成才開始上傳。
- 介面驗收必須同時確認：總進度與本資料夾進度增加、狀態為正在執行、照片與檔名同步、LLM 自然語言逐字顯示、右側縮圖持續累積、逐張上傳總數／待上傳數可見。

## 型號規則同步
- `skills/model_catalog_rules.py` 是一般型號正規化與 FollowMe 系列的唯一共用權威；批次、提示詞、查價、補跑與改名工具不得各自維護另一份規則。
- 官網完整碼只移除前導 `L` 與結尾 `XZW`；自動修正必須是精確命中、唯一尾碼補全，或同尺寸同系列內唯一的有限近似候選。
- FollowMe 共六款正式名稱；只有家族證據時使用 `FollowMe 型號未細分`。所有 FollowMe 都是 Smart 系列，不要求 OSD。
- Pro 必須看到同一台實機或附著牌面的明確 `Pro`；尺寸、價格與共用面板 SKU 不得推導 Pro。
- 與線上回報表同步規則時，先更新共用權威與回歸測試，再同步提示詞、型號表與文件；不得只改其中一處。

## 歷年照片接力與改名規則
- 另一台電腦上的 AI 接手執行時，先讀 `docs/ai_handoff_runbook.md`；那份文件是正式接力執行清單。
- 照片來源根目錄是外部資料，不屬於 Git 專案；`D:\00_歷年商化照片` 只是目前這台電腦的位置，另一台電腦可不同。
- 2K 縮放指大於 2K 時長邊縮到 `2560`，短邊按原比例自然縮放；不裁切、不補白、不硬拉伸。
- 只處理 `.jpg`、`.jpeg`、`.png`；`HEIC`、`WebP` 可以略過並列入審計。
- 2026 年執行時，2025 含以前不做 Samsung 官網價格比對；仍要保留 OCR 讀到的店內價格。
- 當年度有官網比價時，檔名價格欄必須保留 `↑/↓/✓/？`；歷史年度不比價時不加比價符號。
- 改名後照片可以全部輸出到同一層新資料夾，因為檔名已包含年月；若同名，不可覆蓋，需加尾碼。
- 自動接力應沿用既有 Dashboard/Flask 後端 API，不要用滑鼠點擊模擬，也不要重寫 OCR 核心。
