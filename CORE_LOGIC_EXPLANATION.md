# Samsung OCR 核心邏輯開發手冊 (Core Logic & Findings)

本文件記錄了 Samsung Monitor OCR 專案在開發過程中的關鍵發現、邏輯演變與「反幻覺」策略。

## V. 視覺邏輯的演化 (The Evolution of Visual Logic)

### 1. 隧道視野 (Tunnel Vision) 與全局盤點

**發現時間**：v18.49 (案例 852, 431)
**現象**：
當畫面中出現多張並排的價格牌時（例如賣場整排展示），AI 往往會犯「直覺性錯誤」。它會抓取視覺上最顯眼、或距離中心最近的那一張牌，而忽略了它是否與主體尺寸匹配。
例如：主體是 27 吋螢幕，AI 看到正下方的 32 吋標籤 ($6990) 就直接抓取，完全忽略了左側還有一張 27 吋標籤 ($4990)。

**根本原因**：
LLM 的預設推論模式是「線性」的：發現主體 -> 尋找鄰近文字 -> 匹配成功 -> 結束。缺乏「全局觀」。

**解決方案：負片掃描 (Negative Space / Global Inventory)**
我們引入了 **Step 0: 全局盤點**。
強制 AI 在做決定之前，先執行「清點」動作：

> "畫面上共有 3 張牌：Tag A (32", $6990), Tag B (27", $4990), Tag C (24", $3990)。"
> "主體是 27 吋。"
> "結論：Tag B 雖然偏左，但尺寸吻合，故選 B。"

---

### 2. 空間隔離與異色拼接 (Spatial Segregation & Color Mismatch)

**發現時間**：v18.45 - v18.46 (案例 431)
**現象**：
賣場人員常將「型號規格牌」（通常是藍色/粉色）與「價格牌」（通常是黃色）分開張貼。有時兩者物理上貼得很近，但其實是不同張紙。
AI 為了填滿 JSON 欄位，會產生「強行拼接」的幻覺，聲稱「型號 S27... 與價格 5290 都在同一張藍色卡片上」，即便肉眼可見兩者顏色不同且有縫隙。

**解決方案：色系一致性執法 (Color Integrity Enforcement)**
我們不能只依賴 AI 的「結構語意」，必須引入「像素級」的規則：

- **色系檢查**：若 Model 背景是冷色系（藍/白/粉），而 Price 背景是暖色系（高飽和黃），則 **REJECT**。
- **物理邊界**：若兩者之間有陰影、縫隙或紙張邊緣，視為獨立個體。

---

### 3. FollowMe 的托盤獵殺 (FollowMe Tray Hunt)

**發現時間**：v18.47 (案例 768)
**現象**：
FollowMe 機型因其特殊外型（落地支架），常被豁免於傳統標籤檢查。但過度豁免導致 AI 「懶惰」，忽略了放置在中間托盤上的價格貼紙。

**解決方案**：
特例中的特例。一旦識別出 FollowMe（白立架+托盤），啟動 **Tray Scan** 模式，強制掃描托盤平面上的任何微小文字或貼紙。

---

## VI. 後端防護網 (Backend Safety Nets)

### 1. 價格守門員 (Price Guard)

為了防止 AI 把型號的一部分（如 `1000R`, `1500R`）誤讀為價格，我們在後端 Python 層設有硬性攔截：

- **禁忌數字**：`1000`, `1500`, `1800` (常見曲率)。
- **長度限制**：僅接受 4-5 位數的價格 (TWD 正常範圍)。
- **格式清洗**：移除所有非數字字符。

### 2. 資料庫白名單 vs 模糊修復

- **白名單 (Whitelist)**：系統僅接受 `valid_models_list` 中的型號。
- **模糊修復 (Fuzzy Match)**：若 OCR 讀出的型號由 1-2 個字元錯誤（如 `S24F332` vs `S24F532`），且相似度 > 0.7，系統會自動修正為資料庫中的正確型號。
- **Bypass 機制**：針對 FollowMe 等特殊機型，允許繞過白名單檢查，避免因資料庫缺漏或格式問題導致正確識別被誤殺。
